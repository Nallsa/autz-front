const initialState = {
  singUp: false,
  error: null,
  token: null,
};

export default function applecation(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
